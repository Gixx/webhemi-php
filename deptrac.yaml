deptrac:
  paths:
    - ./src
  exclude_files:
    - '#.*Test.*#'
  layers:
    - name: Entity
      collectors:
        - type: directory
          value: src/Entity/.*
    - name: Repository
      collectors:
        - type: directory
          value: src/Repository/.*
    - name: Api
      collectors:
        - type: directory
          value: src/Api/.*
    - name: Controller
      collectors:
        - type: directory
          value: src/Controller/.*
    - name: Security
      collectors:
        - type: directory
          value: src/Security/.*
    - name: Routing
      collectors:
        - type: directory
          value: src/Routing/.*
    - name: SiteHost
      collectors:
        - type: directory
          value: src/SiteHost/.*
    - name: Command
      collectors:
        - type: directory
          value: src/Command/.*
  ruleset:
    Entity:
      - Repository
    Repository:
      - Entity
    Api:
      - Entity
      - Repository
    Controller:
      - Entity
      - Repository
      - SiteHost
      - Routing
      - Api
    Security:
      - Entity
      - Repository
    Routing:
      - Entity
      - Repository
    SiteHost: ~
    Command:
      - Entity
      - Repository
