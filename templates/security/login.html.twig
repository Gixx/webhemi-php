{% extends 'base.html.twig' %}

{% block title %}Sign in — WebHemi{% endblock %}

{% block body %}
{# Admin Theme paints the desktop; LoginPage centers the Retro OS dialog. #}
<div {{ react_component('LoginPage', {
    action: path('app_login'),
    csrfToken: csrf_token('authenticate'),
    csrfFieldName: '_csrf_token',
    emailDefault: last_username,
    error: error,
    bannerUrl: asset('admin/system/banner-dialog-login.gif'),
    errorSoundUrl: asset('admin/sounds/chord.mp3'),
    dingSoundUrl: asset('admin/sounds/ding.mp3')
}) }}></div>
{% endblock %}
